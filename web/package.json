{
  "name": "pneuma-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@pneuma/ui": "workspace:*"
  },
  "scripts": {
    "predev": "bun run --cwd .. sync:icons",
    "prebuild": "bun run --cwd .. sync:icons",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  }
}
